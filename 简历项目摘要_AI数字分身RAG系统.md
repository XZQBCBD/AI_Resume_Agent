# 简历项目摘要

## 一、项目简介与背景

大语言模型在通用对话场景表现突出，但面对个人经历、专业技能等私有知识域提问时，存在知识截断与幻觉生成两大缺陷——模型既不了解用户的真实背景，又倾向于编造看似合理但完全失实的内容。传统招聘筛选依赖人工逐份审阅简历与项目文档，效率低，且无法支撑个性化深度追问。

本项目构建了一个 RAG（检索增强生成）驱动的 AI 数字分身 Agent 系统，核心定位为**具备知识检索、上下文增强与可控生成能力的智能问答 Agent**。系统将简历、项目文档、技术博客、推荐信等非结构化私有资料构建为可检索的外部知识库，用户提问时，Agent 自主执行双路混合检索召回相关知识片段，经 RRF（倒数排名融合）排序后注入 Prompt 上下文，交由 LLM 基于事实生成以第一人称组织的专业回答，模拟候选人在技术面试中的真实应答行为。

项目采用 FastAPI + Streamlit 前后端分离架构，核心 RAG 引擎层与 Web 框架零耦合，已完整实现文档解析（PDF 三级级联引擎含 OCR 降级）、五级语义感知切分与章节类型标注、双路索引构建与持久化、RRF 混合检索与 Min-Max 分数归一化、结构化 Prompt 模板管理、多 LLM Provider 热切换、MD5 哈希变更检测与智能增量同步、索引一致性校验、20 题/6 类意图评估体系（Hit Rate@K + MRR + LLM-as-a-Judge + Self-Reflection 重试 + A/B 权重对比）等全链路工程能力，具备开箱即用与可扩展部署的工程落地水平。

## 二、技术栈

**开发语言与工程基础**：Python 3.10+，遵循分层架构设计模式（引擎层 / 服务层 / 表现层），使用 dataclass 单例集中管理配置（含安全类型转换兜底），Pydantic v2 实现 API 层类型安全校验，模块间通过策略模式、懒加载等设计模式组织，具备完整的工程化代码规范。

**大模型基座**：DeepSeek（deepseek-chat）作为主力模型，兼容 OpenAI（GPT-4o）及任意 OpenAI 协议兼容端点。选型理由：DeepSeek 中文理解能力优异、API 成本约为 GPT-4 的 1/20、国内网络可达；通过统一 `openai` SDK 仅切换 `base_url` 实现多 Provider 无侵入切换，避免供应商锁定，企业级私有化部署模型可零改造接入。

**Prompt 工程**：采用 TOML 模板外部化方案，构建三层 Prompt 架构——`[system]` 段定义 Agent 角色 Persona 与 8 条行为约束（零幻觉最高优先级、第一人称输出、STAR 法则结构化回答、全量整合参考资料、综合类问题策略、技术深度展示、篇幅控制、友好专业语气），`[template]` 段管理含 `{role}{rules}{context}{question}` 占位符的对话模板，`[ui]` 段配置前端交互文案。选型理由：TOML 比 JSON/YAML 注释友好、段落清晰，适合非技术人员协同调整 Agent 行为；Prompt 与代码完全解耦，修改即生效无需重部署。

**嵌入模型**：sentence-transformers/all-MiniLM-L6-v2（384 维 / 90MB），本地 CPU 毫秒级推理。选型理由：对比 BGE-large-zh（1.3GB / 1024 维），在百级文档规模下大模型收益边际递减，轻量化方案满足精度需求的同时大幅降低推理延迟与内存占用；本地运行保障数据不出域。

**向量检索**：NumPy 数组矩阵运算实现余弦相似度检索，配合 JSON 文件管理向量 ID 映射。选型理由：个人知识库文档量在百/千级，NumPy 矩阵乘法暴力计算（底层调用 BLAS 优化）在毫秒级完成全部相似度计算；避免引入 ChromaDB 等外部向量数据库带来的 SQLite/hnswlib 依赖链与版本兼容风险（经实测 Windows 下 ChromaDB Rust FFI 稳定崩溃），检索逻辑完全透明可控。

**关键词检索**：rank-bm25 + jieba 中文分词（73 个自定义技术术语 + 180 个中英文停用词过滤 + 分数阈值），通过 pickle 序列化实现索引持久化。选型理由：BM25 内置文档长度归一化，对"FastAPI""LangChain"等技术专有名词的精确匹配能力远超向量语义检索，与 Dense 通路形成互补；自定义词典解决 jieba 默认分词对领域术语过度切分问题，停用词过滤消除高频无意义词噪声。

**文档解析**：pdfplumber（中文兼容性最优）→ pypdf（BSD 协议回退）→ OCR/pytesseract（扫描版降级）三级级联引擎，配合乱码率自动检测（U+FFFD 占比 > 30% 触发 OCR）+ python-docx + 自研 PlainTextParser，覆盖 PDF/Word/Markdown/TXT 四种格式，策略模式按文件后缀自动路由解析器。

**后端框架**：FastAPI（Starlette 异步核心 + Pydantic v2 自动校验 + Swagger UI 自动生成）+ Uvicorn 部署。选型理由：原生 async/await 异步支持，对比 Flask（同步框架）并发能力更强，对比 Django REST（全栈框架）组件更轻量，适配 Agent 系统的微服务化 API 架构。

**前端框架**：Streamlit 纯 Python 构建 Web UI。选型理由：消除 JS/TS 技术栈上下文切换，对话组件（chat_message / expander / chat_input）均为内置，适合 Agent 应用的快速原型交付与迭代验证。

## 三、技术亮点

**1. 全链路手写 RAG 管道，掌握检索增强生成底层原理**
放弃 LangChain/LlamaIndex 等高层封装框架，从文档加载、五级语义感知切分、嵌入向量生成、双路索引构建、RRF 混合检索融合排序到上下文注入与 LLM 调用，全链路自主实现。每个环节可单步调试、可独立替换、可定向优化。这是 Agent 知识检索能力的根基性建设——掌握底层原理后，使用任何框架都具备降维理解与调优能力。

**2. Dense + Sparse 双路混合检索与 RRF 融合排序**
针对单一向量检索对技术专有名词召回率低、以及两路分数量纲不一致导致融合失准的问题，设计双路召回 + RRF（Reciprocal Rank Fusion）融合架构：向量通路计算查询与全部文档的余弦相似度取候选集，BM25 通路经增强 jieba 分词后评分取候选集，通过 RRF 公式 `Σ 1/(k + rank_i)`（k=60）天然处理异构分数量纲，双路命中自动加权。RRF 排序后再对原始 Dense/BM25 分数做 Min-Max 归一化生成 0~1 可读分数。该方案借鉴 Elasticsearch 8.x+ 工业标准，对异常高分不敏感、比纯 Min-Max 加权更鲁棒，在 20 题/6 类意图测试集上 Hit Rate@5 达 90%。

**3. 五级语义感知文本切分 + 章节类型自动标注**
针对中文场景下固定长度切分在句子中间截断、破坏上下文完整性的问题，自研五级切分策略：① 结构感知（识别列表项 `1.`/`-`/`第X章` 和 Markdown 标题 `##` 作为硬边界）→ ② 自然段落切分（空行正则）→ ③ 超长段落按中英文句末符二次切分 → ④ 短块合并（< 200 字符与相邻块合并）→ ⑤ 相邻块 20% 重叠（100 字符），块长度集中在 200-500 字符理想区间。同时自动检测文档章节标题，为每个 chunk 标注 `section_type`（education/project/skills/experience/self_intro/awards/contact/general），提升下游检索精度。

**4. Agent 行为安全护栏——结构化 Prompt 防幻觉机制**
将 Agent 的安全边界与行为准则编码为 Prompt 层系统级约束：角色锚定为指定 Persona，最高优先级规则为"零幻觉——严格基于参考资料，不存在则诚实说明"，输出规范约束为第一人称 + STAR 法则 + 全量整合参考资料 + 综合类问题策略 + 篇幅控制。这一设计将"基于事实生成"与"未知即拒绝"从模型的自发行为转化为 Prompt 编码的确定性约束，是 RAG Agent 防幻觉的最后一道防线，也是 Prompt Engineering 在 Agent 可靠性方向的核心实践。

**5. PDF 三级级联解析引擎 + 乱码率自动检测**
针对中文 PDF 解析中 pypdf 对非标准 CMap 编码支持差的问题，设计三级级联策略：pdfplumber（中文兼容性最优）→ pypdf（BSD 协议回退）→ OCR/pytesseract（扫描版降级）。核心创新是加入文本质量检测函数——统计 U+FFFD 替换字符占比，乱码率 > 30% 自动触发 OCR 降级，同时保证非扫描版 PDF 的解析效率。后处理清洗移除页码残留和多余空白，确保进入检索管道的文本干净完整。

**6. 多 LLM Provider 热切换——解除模型供应商锁定**
基于 OpenAI 兼容协议（`/v1/chat/completions`）实现 Provider 无关的 LLM 接入层：DeepSeek / OpenAI / 自定义端点三通道共享同一调用路径，仅需修改 `base_url` + `model` 参数即可切换。前端侧边栏下拉框实时切换 Provider，后端懒加载客户端按需重建连接。企业内部私有化部署的模型（vLLM / Ollama / OneAPI 网关）可零改造接入，这是企业级 Agent 应用不被单一模型供应商锁定的标配架构设计。

**7. 系统化检索评估体系——20 题/6 类意图 + LLM-as-a-Judge**
构建了完整的检索质量评估框架：20 题测试集覆盖自我介绍/项目经历/技术栈/教育背景/团队角色/求职意向 6 类意图，每题含 expected_keywords 用于自动评估。支持 Hit Rate@K + MRR 高级指标、5 种权重预设（balanced/semantic_first/keyword_first/dense_only/sparse_only）A/B 一键对比、LLM-as-a-Judge 三维打分（准确性/相关性/完整性 1-5 分）及 Self-Reflection 自动重试机制（低分回答自动纠错）。这是将 RAG 系统从"能用"推向"可靠"的关键工程实践。

**8. MD5 哈希变更检测与智能索引同步**
借鉴 Git object hash 机制，实现基于 MD5 文件哈希的索引变更追踪：每次构建后保存 file_hashes.json 快照，`--sync` 模式自动对比哈希检测增/删/改，有变更则全量重建、无变更则跳过；`--status` 零开销查看索引状态；`--sync --dry-run` 预览操作。增量模式通过 np.vstack 追加新向量并去重已有 ID，避免重复嵌入计算。内置索引一致性校验——启动时自动检测 embeddings/BM25/chunks_meta 各文件记录数是否一致，不一致时自动裁剪至安全上限并告警提示重建。

**9. 三层分离架构与完整工程化落地能力**
引擎层（RAG Core）、服务层（FastAPI）、表现层（Streamlit）严格解耦：引擎层零 Web 依赖，可被 CLI 脚本、API 服务、单元测试独立调用；服务层仅做请求校验与错误格式转换，不含业务逻辑。配套实现：配置单例化集中管理（含安全类型转换兜底）、HuggingFace 缓存路径自动劫持至非系统盘、LLM 客户端懒加载、BM25 自定义词典与停用词过滤、NumPy 矩阵运算加速检索、防御性编程（空目录自动生成 5 个中文示例文件 / 单文件解析失败不中断 / 各层级分级错误码与兜底响应），形成可维护、可测试、可扩展的 Agent 系统工程基座。
