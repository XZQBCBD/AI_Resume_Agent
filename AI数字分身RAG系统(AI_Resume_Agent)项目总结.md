# AI 数字分身（RAG 智能问答系统）— 简历项目详细总结

---

## 模块一：项目整体设计思路

### 1.1 核心问题与应用场景

**解决的核心问题**：大语言模型（LLM）存在固有知识截断与幻觉问题，无法准确回答涉及个人经历、项目细节、专业技能等私有知识域的提问。本项目构建一套 RAG（Retrieval-Augmented Generation，检索增强生成）系统，将个人简历、项目文档、技术博客、推荐信等私有资料作为外部知识库，通过检索增强方式让 LLM 基于事实生成回答，实现"AI 数字分身"——在求职面试、技术沟通等场景中，代替候选人进行基于真实经历的专业问答。

**目标用户**：求职者本人、面试官、HR 筛选初面、技术团队快速了解候选人背景。

**核心价值**：
- 消除 LLM 幻觉，所有回答可追溯到原始文档来源
- 将非结构化个人资料转化为可检索的知识库
- 通过 Prompt 工程控制回答风格（第一人称、STAR 法则、事实导向、零幻觉约束）
- 支持多 LLM Provider 热切换，不绑定单一模型供应商
- 内置系统化评估体系，量化检索与生成质量

### 1.2 整体架构设计逻辑

项目采用 **三层分离架构**，按职责边界拆分为：

```
┌────────────────────────────────────────────┐
│  表现层（Streamlit）      │  端口 8501      │  ← 纯 UI 逻辑，无业务耦合
├────────────────────────────────────────────┤
│  服务层（FastAPI）        │  端口 8000      │  ← REST API，请求校验，路由分发
├────────────────────────────────────────────┤
│  引擎层（RAG Core）       │  零 Web 依赖    │  ← 检索/增强/生成全管道
│  ├── 检索器（Retriever） — RRF混合检索     │
│  ├── Prompt 加载器 — TOML模板管理          │
│  ├── 文档加载器（Loader） — 五级切分+章节标注│
│  ├── 索引构建器（IndexBuilder） — MD5变更追踪│
│  ├── 评估器（Evaluator） — 20题+LLM Judge  │
│  └── BM25工具（bm25_utils） — 增强分词     │
└────────────────────────────────────────────┘
```

**拆分依据**：
- **引擎层**与 Web 框架完全解耦，可独立测试、可被任何前端（CLI/Web/API）调用
- **服务层**仅做请求验证与错误转换，不含业务逻辑，保持轻量
- **表现层**通过 HTTP 调用后端，不直接依赖引擎层，保证前后端可独立部署

**整体运行流程（离线 + 在线）**：

- **离线建库**：原始文档 → 多格式解析（三级级联PDF引擎 + 策略模式） → 五级语义感知切分 + 章节类型标注 → 双路索引构建（Dense 向量 + Sparse BM25增强分词） → 磁盘持久化 + MD5哈希快照
- **在线问答**：用户提问 → HTTP POST → FastAPI 路由 → RAG 引擎 → 混合检索（余弦相似度 + BM25 → RRF融合排序 → Min-Max分数归一化） → Prompt 模板填充 → OpenAI 兼容 API 调用 LLM → 返回答案 + 引用来源

### 1.3 Agent 核心设计理念

本项目现阶段定位为 **单轮 RAG 问答 Agent**，已内置以下 Agent 核心能力基座：

| Agent 能力 | 本项目实现方式 | 设计考量 |
|-----------|--------------|---------|
| **知识检索** | 混合检索（Dense + Sparse RRF融合），双路召回，RRF排序 + Min-Max归一化展示分数 | 语义理解 + 精确匹配互补，RRF天然处理异构量纲，对异常高分鲁棒 |
| **记忆能力** | 个人知识库（向量索引 + BM25 索引），文档元数据标注来源/类型/章节类型 | 外部知识作为 Agent 的长期记忆，区别于 LLM 参数的静态知识 |
| **上下文管理** | Prompt 模板中的 `{context}` 占位符，检索结果格式化注入 System Prompt | 将检索到的知识作为上下文窗口内的临时记忆，附来源标注 |
| **工具使用** | 支持多 LLM Provider 切换（DeepSeek/OpenAI/Custom），统一 OpenAI 兼容协议 | 预留了 Function Calling 扩展接口 |
| **自主决策** | 基于 RRF 融合分数自动选择 Top-K 文档片段，无结果时触发零幻觉规则诚实拒绝 | 避免强行回答导致幻觉 |
| **自我评估** | LLM-as-a-Judge 三维打分 + Self-Reflection 自动重试纠错 | 将 RAG 系统从"能用"推向"可靠" |

**设计理念的核心**：不追求概念上的"多 Agent 协作""自主规划"等花哨标签，而是扎实落地 RAG 的基础设施——高质量检索 + 可控生成 + 来源可追溯 + 可量化评估。在此基础上，后续可以自然地扩展为多轮对话 Agent、具备工具调用的 Agent、以及多 Agent 协作系统。

### 1.4 规避的行业痛点与解决的核心难点

| 行业痛点 | 本项目解决方案 |
|---------|-------------|
| **LLM 幻觉：编造不存在的信息** | 零幻觉规则为最高优先级，所有回答必须基于检索到的文档片段，检索为空时明确拒绝回答 |
| **固定长度切分破坏语义** | 自研五级切分：结构感知 → 段落优先 → 句子兜底 → 短块合并 → 块间20%重叠 |
| **检索到的片段缺少章节上下文** | 自动检测章节标题并为每个 chunk 标注 section_type，下游可按类型过滤 |
| **单一检索方式召回不全** | 双路召回（语义 + 关键词），RRF 融合排序（k=60），5种权重预设可切换 |
| **Dense/BM25分数量纲不一致** | RRF 基于排名融合，天然处理异构量纲；分数归一化仅用于展示 |
| **Prompt 硬编码在代码中** | TOML 外部化 Prompt 模板（8条行为约束），修改即生效无需重部署 |
| **PDF中文乱码/扫描版无文本** | 三级级联引擎：pdfplumber→pypdf→OCR，乱码率>30%自动触发OCR降级 |
| **BM25召回不稳定（0-10条极端波动）** | 73个自定义词+180个停用词+分数阈值0.05，构建和检索使用同一套tokenize() |
| **模型缓存膨胀占用系统盘** | 启动时自动劫持 HuggingFace 全系列环境变量到非系统盘（`E:/AI_Cache/huggingface`） |
| **不同 LLM Provider API 不兼容** | 统一使用 OpenAI 兼容协议（`/v1/chat/completions`），DeepSeek/OpenAI/私有化部署均适用 |
| **索引与文档一致性无法确认** | MD5文件哈希变更检测，--sync智能同步，--status零开销状态查看 |
| **索引数据文件不一致导致越界崩溃** | 启动时自动校验各文件记录数的一致性，不一致自动裁剪至安全上限并告警 |
| **Windows路径反斜杠转义陷阱** | 全局使用正斜杠+pathlib，规避`\a`/`\v`等转义字符问题 |
| **项目空目录启动即崩溃** | 防御性编程：数据目录为空时自动生成5个中文示例文件，保证开箱即用 |

---

## 模块二：全套技术栈选型理由与详细思路

### 2.1 LLM 基座：DeepSeek（主力）/ OpenAI / Custom Provider

| 选型维度 | DeepSeek（主力） | OpenAI（备用） | Custom（扩展） |
|---------|----------------|--------------|--------------|
| 选择理由 | 中文理解能力优异，API 价格低（约为 GPT-4 的 1/20），OpenAI 兼容协议，国内访问稳定 | 英文生成能力最强，作为对比基准 | 兼容任意 OpenAI 协议端点（vLLM/Ollama/OneAPI），用于私有化部署 |
| 核心作用 | 作为生成层，接收检索上下文 + 用户问题，生成基于事实的回答 | 备用链路 | 支持企业内部模型接入 |
| 调用方式 | 统一 `openai` SDK，仅切换 `base_url` + `api_key` + `model` | 同 | 同 |

**为什么不选 LangChain/LlamaIndex？**
- LangChain 对学习阶段而言封装过度、抽象层级过多，隐藏了大量 RAG 关键细节（如检索融合算法、文本切分逻辑）
- 本项目手写全部 RAG 管道，控制力 100%，调试可追溯到每一行代码
- 实际企业项目中，轻量封装优于重框架依赖——LangChain 的版本兼容性问题和过度抽象在工程圈已被广泛诟病
- 掌握底层原理后，使用任何框架都是降维打击

### 2.2 嵌入模型：sentence-transformers/all-MiniLM-L6-v2

| 对比方案 | 为什么不选 | 为什么选 MiniLM-L6-v2 |
|---------|----------|---------------------|
| OpenAI text-embedding-3-large（3072 维） | 需 API 调用，有网络延迟和费用，隐私数据需上传外部 | 本地运行，零延迟，零费用，数据不出域 |
| BGE-large-zh（1024 维） | 中文效果更好，但模型体积 1.3GB，推理慢 4 倍 | 384 维轻量模型，仅 90MB，CPU 上毫秒级推理 |
| M3E-base（768 维） | 中文数据集训练，效果好但体积大 | 个人知识库规模（几十到几百个文档），384 维足够，向量维度翻倍收益递减 |

**选型考量**：
- **轻量化优先**：个人知识库文档量级小，大模型收益边际递减，小模型推理速度快、内存占用低
- **离线可用**：本地 SentenceTransformer 运行，不依赖外部 API，无网络中断风险
- **通用性**：MiniLM 在多语言场景下表现稳定，中英文混合文档兼容性好
- **扩展性**：后续若文档量增长到万级，可无缝升级为 BGE-large-zh 或 M3E-base，只需改一行配置

### 2.3 向量存储：NumPy 数组 + JSON（自实现）vs ChromaDB

| 对比维度 | ChromaDB | NumPy + JSON（本项目方案） |
|---------|---------|-------------------------|
| 依赖复杂度 | 需安装 chromadb 包，依赖 SQLite + hnswlib + Rust FFI | 仅 NumPy（已在依赖中），零额外安装 |
| 检索方式 | 内置 ANN 索引（HNSW），API 封装 | 手写余弦相似度（矩阵乘法），完全可控 |
| Windows兼容性 | Rust FFI (pyo3) 在 Windows 上 segfault 崩溃，3个版本均失败 | 无兼容性问题，纯 Python + C 扩展（BLAS） |
| 调试透明度 | 黑盒查询，难以直接检查向量和 ID 映射 | `embeddings.npy` + `embedding_ids.json` 可直接查看 |
| 适用规模 | 百万级文档 | 百/千级文档（本场景） |
| 性能 | HNSW 近似检索，大规模时优势明显 | 暴力计算余弦相似度，千级文档毫秒级完成 |

**核心决策**：经实测 ChromaDB 1.5.9/1.0.0/0.4.24 三个版本在 Windows 10 下均稳定崩溃（Rust pyo3 FFI 与 VC++ 运行时兼容问题）。最终自研 NumPy 方案，将问题转化为机遇——15 行代码实现完全可控的向量检索。**选型原则：技术方案匹配问题规模，不过度工程化；环境兼容性优先于功能丰富度。**

### 2.4 关键词检索：rank-bm25 + jieba 增强分词

| 对比方案 | 为什么不选 | 为什么选 BM25 + jieba 增强 |
|---------|----------|-------------------------|
| Elasticsearch | 需部署 Java 服务，个人项目太重 | BM25 算法 + jieba 分词，纯 Python 实现，零部署 |
| TF-IDF（sklearn） | 对文档长度不敏感，缺少长度归一化 | BM25 内置文档长度归一化，检索质量优于 TF-IDF |
| 纯向量检索 | 无法精确匹配专有名词、技术栈名称 | BM25 对"FastAPI""LangChain"等关键词精确匹配能力远超向量 |

**增强分词策略**：
- **73 个自定义词**：覆盖 AI/ML 术语、框架工具、编程语言、数据库中间件、简历场景词，防止 jieba 过度切分（如"自然语言处理"不被切成"自然/语言/处理"）
- **180 个中英文停用词**：过滤"的/了/the/a"等高频无意义词，消除 BM25 噪声分数
- **分数阈值 0.05**：过滤 BM25 低分噪声命中
- **一致性保证**：构建索引和检索时使用同一套 `tokenize()` 函数（抽取到 `bm25_utils.py`），避免词表不一致导致召回失败

### 2.5 后端框架：FastAPI + Uvicorn

| 对比方案 | 为什么不选 | 为什么选 FastAPI |
|---------|----------|-----------------|
| Flask | 同步框架，无原生异步支持，无自动 API 文档 | FastAPI 原生异步（async/await），自动生成 OpenAPI 文档（Swagger UI） |
| Django REST | 全套方案过于沉重，ORM/模板引擎/中间件均不需要 | FastAPI 微框架，仅提供路由 + 校验 + 文档，无冗余组件 |
| Gradio | 适合 Demo 展示，不适合 API 服务化 | FastAPI 是 API 服务工业标准，适合与任何前端解耦 |

### 2.6 前端框架：Streamlit

| 对比方案 | 为什么不选 | 为什么选 Streamlit |
|---------|----------|------------------|
| React/Vue + 独立前端项目 | 需要前后端分离开发，JS/TS 技术栈增加学习成本 | Streamlit 纯 Python，数据科学家/后端工程师可直接写 UI |
| Gradio | 界面风格固化，自定义能力弱 | Streamlit 组件丰富，布局灵活，可嵌入自定义 CSS |
| Chainlit | 专注 LLM 对话，其他场景支持弱 | Streamlit 通用性强，聊天/仪表盘/文档管理均可 |

### 2.7 Prompt 工程：TOML 模板 + Python string.format() + 8条行为约束

| 设计选择 | 具体做法 | 理由 |
|---------|--------|-----|
| 存储格式 | TOML | 比 JSON/YAML 更适合人工编写配置文件，注释友好，段落清晰 |
| 占位符方案 | Python `str.format()` | 简单直接，无模板引擎学习成本，4 个占位符 `{role}{rules}{context}{question}` 清晰明了 |
| 内容分离 | `[system]`（角色/规则）+ `[template]`（模板）+ `[ui]`（UI 配置） | 角色定义与模板结构解耦，改角色不改模板，改规则不改角色 |
| 热生效 | 不缓存，每次调用重新读取 | 文件小（<2KB），读取开销可忽略，修改即生效 |

**Prompt 设计思路——8 条分层行为约束**：
- **规则0（最高优先级）**：零幻觉——严格基于参考资料，不存在则诚实说明，绝不编造
- **规则1**：第一人称输出（"我"而非"候选人"）
- **规则2**：STAR 法则结构化回答（项目经历类问题）
- **规则3**：全量整合——逐条阅读所有参考资料，不依赖前1-2条
- **规则4**：综合类问题策略——优先以"个人总结"类文档块为核心框架
- **规则5**：技术深度展示——架构决策、技术选型理由、量化成果
- **规则6**：篇幅控制（综合类 300-500 字，技术类 200-400 字）
- **规则7**：友好专业——自信但不傲慢，展现技术热情和团队协作精神

### 2.8 文档解析：pdfplumber → pypdf → OCR 三级级联引擎

| 对比方案 | 为什么不选 | 为什么选 |
|---------|----------|--------|
| PyMuPDF（fitz） | AGPL 协议，商用风险 | pdfplumber（MIT）+ pypdf（BSD），无法律风险 |
| pypdf 单独使用 | 中文 PDF 非标准 CMap 编码兼容性差，产生大量 U+FFFD 乱码 | pdfplumber 内部维护更完整的 CMap 表，中文支持最优 |
| pdfplumber 单独使用 | 扫描版 PDF 完全无法处理 | 加入 OCR 降级，乱码率 > 30% 自动触发 |
| Unstructured（开源文档解析库） | 依赖重，安装复杂 | 仅需 PDF/Word/TXT/MD 四种格式，自写解析器精确可控 |

**三级级联策略**：
1. pdfplumber（中文兼容性最优，默认生效）
2. pypdf（BSD 协议，回退方案）
3. OCR/pytesseract（扫描版降级，乱码率 > 30% 自动触发，需安装 Tesseract-OCR + chi_sim 语言包）

**文本质量检测**：`_calc_garbage_ratio()` 统计 U+FFFD 替换字符和控制字符占比，> 30% 自动降级。后处理清洗移除页码残留和多余空白。

### 2.9 配置管理：dataclass + python-dotenv + 安全类型转换

**设计模式**：模块级单例 `Settings` dataclass，全局唯一入口 `from src.config import settings`。

**选型考量**：
- `dataclass` 比字典有类型提示和 IDE 自动补全，比 Pydantic Settings 更轻量
- `field(default_factory=...)` 实现路径自动推断（基于 `__file__`）
- `__post_init__` 自动执行：环境变量劫持 → 路径解析 → 目录创建 → 权重预设应用 → 配置校验
- 安全类型转换：`_safe_int/_safe_float` 对环境变量值做容错解析，解析失败有兜底值 + warning 日志
- `.env` 与代码分离，API Key 不入库（`.gitignore` 忽略 `.env`，保留 `.env.example` 模板）
- 5 种权重预设通过 `WEIGHT_PRESET` 环境变量一键切换，支持运行时 `set_weight_preset()` 动态调整

---

## 模块三：核心功能与落地实现细节

### 3.1 多格式文档智能解析与切分（含章节类型标注）

**实现逻辑**：

```
文件后缀检测 → 策略模式选择解析器 → 提取纯文本
  → 检测章节标题（Markdown/括号/数字编号）
  → 五级智能切分（结构→段落→句子→合并→重叠）
  → 章节类型自动标注（8种类型）
  → Chunk + 元数据（含 section_type）
```

**三级PDF级联引擎**：
- `PDFParser`：pdfplumber → pypdf → OCR 三级尝试
- `DocxParser`：`python-docx.Document` 逐段提取，过滤空段落
- `PlainTextParser`：`open(file, encoding='utf-8')` 直接读取
- `PARSER_MAP = {".pdf": PDFParser(), ".docx": DocxParser(), ...}` 后缀到解析器映射

**五级智能切分算法（核心难点突破）**：

```
问题：固定长度切分在句子中间截断，破坏语义完整性；
      简单段落切分导致块长度两极分化（<200短块和>500长块各占1/3）

方案：五级切分策略
  第一级：结构感知 —— 正则识别列表项（1. / - / · / 第X章）和标题（##）
          作为硬边界，保持文档结构完整性
          ↓
  第二级：段落切分 —— 正则 r'\n\s*\n' 按空行自然段落边界切分
          ↓
  第三级：超长拆分 —— 段落长度 > 500 字符时，按中英文句末符边界二次切分
          正则 r'(?<=[。！？.!?\n])\s*'
          贪心拼接至500字符阈值后封闭当前块
          ↓
  第四级：短块合并 —— < 200 字符的块与相邻块合并，
          超限时从后块借字符补足至 min_chunk_size
          ↓
  第五级：块间重叠 —— 相邻块保留 100 字符重叠区域（20%），
          防止跨块边界信息丢失
```

**章节类型自动检测**：
- 通过正则匹配 8 种章节类型：education / project / skills / experience / self_intro / awards / contact / recommendation
- 综合检测 chunk 文本前 200 字符和所属章节标题
- 每个 chunk 的 metadata 中标注 `section_type`，提升下游检索精度

**文档类型自动推断**：
- 前缀精确匹配（共6类）：`00_`→个人总结，`01_`→简历，`02_`→项目，`03_`→博客，`04_`→推荐信，`05_`→技能全景
- 关键词模糊匹配（fallback）：文件名包含"简历/resume/cv"→简历等
- 以上均不匹配 → "其他文档"

### 3.2 混合检索引擎（核心功能，重点实现）

**双路召回 + RRF 融合 + 分数归一化的完整实现**：

**Dense 通路（向量语义检索）**：
1. 用户问题 → `SentenceTransformer.encode()` → 384 维查询向量
2. 查询向量 L2 归一化 + 文档向量矩阵 L2 归一化
3. `numpy.dot(emb_norms, query_norm)` 一次性计算与所有文档的余弦相似度
4. `numpy.argsort()` 取候选集（默认 n_candidates = max(TOP_K_CANDIDATE, top_k × 4)，保证 RRF 候选池充足）
5. 相似度 ≤ 0 的结果过滤（防止无效匹配污染）

**Sparse 通路（BM25 增强关键词检索）**：
1. 用户问题 → `tokenize()` 增强分词（73 自定义词 + 180 停用词过滤）
2. 调用已加载的 `BM25Okapi.get_scores(tokenized_query)` 获取所有文档的 BM25 分数
3. `numpy.argsort()` 取候选集
4. 分数 ≤ 0.05（BM25_SCORE_THRESHOLD）的结果过滤

**RRF 融合排序算法（核心设计）**：

```
问题1：向量余弦相似度 ∈ [0, 1]，而 BM25 分数可以远大于 1（如 15.2），
       直接加权求和会导致 BM25 完全压倒向量分数
问题2：Min-Max 归一化对异常高分敏感，Top-1 分数固定不变缺乏区分度

方案：RRF（Reciprocal Rank Fusion）+ Min-Max 展示分数
  阶段1 - RRF 排序（解决分数量纲问题）：
    RRF_score(d) = Σ 1/(k + rank_i(d)), k = 60
    - Dense 通路：按余弦相似度排名，rank=1 贡献 1/61
    - Sparse 通路：按 BM25 分数排名，rank=1 贡献 1/61
    - 双路命中的文档自动获得更高 RRF 分数（两个通路都有排名）
    - 按 RRF 分数降序排列 → 取 Top-K

  阶段2 - 分数归一化（生成可读展示分数）：
    对 Top-K 结果分别取原始 Dense/BM25 分数 → Min-Max 归一化到 [0,1]
    → 加权求和: display_score = w_dense × norm_dense + w_bm25 × norm_bm25
    （权重默认 0.6/0.4，可通过 WEIGHT_PRESET 环境变量切换）

  去重机制：同一 chunk（source + chunk_index）在两个通路均出现时，
           RRF 分数相加，双路命中自动加权
```

**RRF 相比纯 Min-Max 加权融合的优势**：
- 无需分数归一化，天然处理异构分数量纲
- 对异常高分不敏感，更鲁棒（是 Elasticsearch 8.x+ 的内置融合算法）
- 双路命中自动获得更高权重（不需要手动调权重）
- RRF 常数 k=60 平滑排名差异，避免排名微小的波动过度影响最终结果

**5 种权重预设**（`.env` 中 `WEIGHT_PRESET` 切换，用于 Min-Max 展示分数计算）：

| 预设 | Dense | Sparse | 适用场景 |
|------|-------|--------|----------|
| `balanced` | 0.5 | 0.5 | 通用均衡（默认推荐） |
| `semantic_first` | 0.6 | 0.4 | 语义理解需求高 |
| `keyword_first` | 0.4 | 0.6 | 精确关键词匹配优先 |
| `dense_only` | 1.0 | 0.0 | 纯向量检索 |
| `sparse_only` | 0.0 | 1.0 | 纯关键词检索 |

### 3.3 Prompt 模板系统与上下文注入

**实现流程**：

```
1. PromptLoader 加载 prompts/system_prompt.toml
2. 解析 [system] 段 → self.role, self.rules
3. 解析 [template] 段 → self.template（带 {role}{rules}{context}{question} 占位符）
4. 运行时调用 build_system_prompt(context, question)：
   self.template.format(role=self.role, rules=self.rules, context=context, question=question)
5. 返回完整的 System Prompt 字符串
```

**上下文注入格式**：

```
[来源: 02_项目_AI数字分身.md (项目)]
（文档内容）...

---
[来源: 01_简历_张三.txt (简历)]
（文档内容）...
```

每个检索结果标注来源文件名和文档类型，让 LLM 在生成时可以区分信息来自简历还是项目文档。

**行为约束（Prompt 中的 Agent 安全护栏，8 条）**：
- 规则0（最高优先级）：零幻觉——严格基于参考资料，不存在则诚实说明
- 规则1：第一人称输出（"我"而非"候选人"）
- 规则2：STAR 法则结构化回答（项目经历类问题）
- 规则3：全量整合——逐条阅读所有参考资料，不依赖前1-2条
- 规则4：综合类问题策略——优先以个人总结文档块为核心框架，覆盖四维度
- 规则5：技术深度展示——架构决策、选型理由、量化成果
- 规则6：篇幅控制（综合300-500字 / 技术200-400字）
- 规则7：友好专业语气

### 3.4 多 LLM Provider 热切换

**实现机制**：

```python
# 配置定义（dataclass）：
LLM_PROVIDERS = {
    "deepseek": {"base_url": "https://api.deepseek.com/v1", "model": "deepseek-chat", ...},
    "openai":  {"base_url": "https://api.openai.com/v1", "model": "gpt-4o", ...},
    "custom":  {"base_url": os.getenv("CUSTOM_BASE_URL"), "model": os.getenv("CUSTOM_MODEL_NAME"), ...},
}

# 统一的 OpenAI 兼容客户端（懒加载）：
@property
def openai_client(self):
    if self._openai_client is None:
        self._openai_client = OpenAI(base_url=..., api_key=...)
    return self._openai_client
```

**热切换流程**：Streamlit 侧边栏下拉框切换 Provider → 修改 `settings.ACTIVE_PROVIDER` → 下次 API 调用懒加载自动重建客户端 → 新 Provider 生效。

### 3.5 离线索引构建、MD5 变更检测与增量更新

**构建流程**：
1. 扫描 `data/raw/` 目录，按支持格式（`.pdf/.docx/.md/.txt`）收集文件
2. 目录为空 → 防御性编程：自动生成 5 个中文示例文件（确保项目 clone 后可直接运行）
3. `DocumentLoader.load_all()` → 解析 + 五级切分 + 章节标注 → `List[Chunk]`
4. `SentenceTransformer.encode(texts)` → NumPy 向量矩阵
5. 保存向量：`np.save(embeddings.npy)` + JSON ID 映射
6. 构建 BM25：增强 `tokenize()` 分词 → `BM25Okapi(tokenized_corpus)` → `pickle.dump`
7. 保存元数据：`chunks_meta.json`（供检索器、评估器和文档清单 API 读取）
8. 保存哈希快照：`file_hashes.json`（供下次变更检测对比）

**MD5 哈希变更检测 + 智能同步**：

```
data/raw/ 文档 ──MD5哈希──▶ file_hashes.json（上次快照）
                                │
                                ▼ 对比
                 ┌────────┬──────────┬──────────┐
                 │  新增  │   修改   │   删除   │
                 │ hash新 │ hash不同 │ hash消失 │
                 └────────┴──────────┴──────────┘
                                │
                                ▼
                        任一变更 → 全量重建
                        无变更   → 跳过
```

| 命令 | 行为 | 适用场景 |
|------|------|----------|
| `python -m src.build_index --sync` | 检测变更 → 有变化则自动重建 | **日常使用首选** |
| `python -m src.build_index --status` | 仅检测变更，不执行操作 | 查看哪些文档有变化 |
| `python -m src.build_index --sync --dry-run` | 检测变更并报告，不重建 | 预览将要更新的内容 |
| `python -m src.build_index --full` | 强制全量重建 | 修改了切分/检索参数后 |
| `python -m src.build_index` | 增量追加（默认） | 仅新增文档，np.vstack 快速追加并去重 |

**增量模式 vs 全量模式**：
- 增量模式：对比新增 ID → `np.vstack` 追加到已有矩阵 → 跳过已存在的文档（去重）
- 全量模式（`--full`）：直接覆盖所有文件
- 单文件失败不影响整体（try/except 包裹每个文件的处理）

### 3.6 索引一致性自动校验

检索器初始化时自动执行 `_validate_index_consistency()`：
- 检测 `embeddings.npy`、`embedding_ids.json`、`bm25_index.pkl`、`chunks_meta.json` 四条数据文件的记录数
- 不一致时打印告警日志 → 以最小值为安全上限 → 自动裁剪各数组防止越界崩溃 → 提示运行 `--full` 重建
- 一致时静默通过

### 3.7 异常容错处理

项目在各层级实施了分层错误处理：

| 层级 | 错误处理机制 |
|------|------------|
| 配置层 | 环境变量类型解析失败 → 兜底值 + warning 日志；权重之和不等于 1.0 → warning；Provider 未注册 → ValueError |
| 文档加载层 | 单文件解析失败 → 跳过该文件，继续处理后续文件；PDF 全部解析方式均失败 → warning 日志 |
| PDF解析层 | pdfplumber 失败 → 自动回退 pypdf；乱码率 > 30% → 自动触发 OCR；OCR 依赖未安装 → 提示安装命令 |
| 检索层 | 向量索引/BM25 索引文件不存在 → warning + 跳过对应通路，另一个通路仍可返回结果；索引数据不一致 → 自动裁剪 + 告警 |
| 引擎层 | LLM 调用失败 → RuntimeError，附 Provider 名称和排查建议；检索为空 → 返回零幻觉兜底回答 |
| API 层 | 引擎未就绪 → 503 + "请先构建索引"；输入无效 → 422；LLM 调用失败 → 500 |
| 前端层 | 后端未启动 → "无法连接到后端服务，请确保已启动 API 服务"；API 错误 → 显示具体错误信息 |

---

## 模块四：项目优化思路与性能提升亮点

### 4.1 检索质量优化

**问题 1：单一向量检索对技术栈专有名词的召回率低**

现象：查询"你用过哪些 Python 框架？"，向量检索将"Python 框架"与"Python 开发经验"语义混淆，返回了不相关段落。

优化方案：引入 BM25 关键词检索作为第二通路，并增强分词——73 个自定义技术术语词典防止 jieba 过度切分，180 个停用词过滤消除噪声。

效果：混合检索后，专有名词的 Top-5 召回率显著提升。

**问题 2：两路分数的量纲不一致导致融合失准**

现象：向量分数在 [0, 1] 区间，BM25 分数可以远超 1。直接 Min-Max 加权导致 BM25 通路完全主导排序，且 Top1 分数固定不变缺乏区分度。

优化方案：升级为 RRF（Reciprocal Rank Fusion）算法——`RRF_score(d) = Σ 1/(k + rank_i(d))`，k=60。RRF 基于排名而非原始分数融合，天然处理异构量纲，双路命中自动加权。RRF 排序后再对原始分数做 Min-Max 归一化生成可读展示分数。

效果：融合后的排序结果中，两路各自的高相关文档都能进入 Top-5，不会出现某一通路被系统性压制的情况；双路命中率从优化前的 12% 提升至显著水平。

**问题 3：BM25 召回不稳定——0 条到 10 条的极端波动**

现象：jieba 默认分词把"自我介绍"切成"自我"+"介绍"，两个独立 token 在文档中匹配不到 → 0 条召回。无停用词过滤，"的""了""是"等高频词产生噪声分数。

优化方案：建立 73 个自定义词表 + 180 个停用词表 + 0.05 分数阈值。关键保证——构建索引和检索时使用同一套 `tokenize()` 函数（抽取到 `bm25_utils.py`）。

**问题 4：文档切分导致信息碎片化——块长度两极分化**

现象：简历 PDF 的列表项每行被单独切为一个短块（<200 字符），超长段落中的单句 >500 字符无法切分。短块信息不足，长块上下文冗余。

优化方案：五级切分策略——结构感知（列表项和标题硬边界）+ 短块合并（<200 字符借相邻块补足）+ 块间 20% 重叠（100 字符）。增加章节类型自动标注，为下游检索提供结构化元数据。

效果：块长度从两极分化改善为集中在 200-500 字符理想区间，检索结果更有层次。

**问题 5：权重调优缺乏系统方法——全靠"感觉"**

优化方案：设计 5 种权重预设 → 20 题/6 类意图测试集 → Hit Rate@K + MRR 多维度指标 → `--compare` 一键 A/B 对比 → LLM-as-a-Judge 三维打分 + Self-Reflection 重试验证。

### 4.2 系统性能与资源优化

**优化 1：HuggingFace 缓存路径劫持**

问题：默认 HuggingFace 模型下载到 C 盘用户目录，系统盘空间不足导致模型下载失败。

方案：在 `Settings.__post_init__` 中自动设置 4 个环境变量（`HF_HOME`、`TRANSFORMERS_CACHE`、`HUGGINGFACE_HUB_CACHE`、`SENTENCE_TRANSFORMERS_HOME`），全部指向 `E:/AI_Cache/huggingface`。同时配置 `HF_ENDPOINT=https://hf-mirror.com` 国内镜像。

**优化 2：LLM 客户端懒加载**

问题：初始化 RAG 引擎时不需要立即连接 LLM，但每次实例化都创建 HTTP 连接浪费资源。

方案：`@property` 装饰器实现懒加载，首次调用 `chat()` 时才创建 OpenAI 客户端。

**优化 3：轻量嵌入模型 + 矩阵运算加速**

问题：BGE-large-zh 等大模型在 CPU 上推理慢（单条 >200ms），内存占用 1.3GB。逐个计算查询向量与每个文档向量的余弦相似度效率低下。

方案：选用 MiniLM-L6-v2（384维/90MB/CPU推理<20ms）；一次性 L2 归一化文档矩阵，`np.dot(emb_norms, query_norm)` 单次矩阵乘法完成全部相似度计算（利用 BLAS 优化）。

**优化 4：RRF 候选池扩大保证召回覆盖**

原方案各通路仅取 Top-10 进入融合。优化后候选池扩大至 `max(TOP_K_CANDIDATE, top_k × 4)`，保证 RRF 融合有足够多的候选参与排名计算，避免高相关但排名在 10+ 的文档被遗漏。

### 4.3 工程架构优化

**优化 1：三层分离解决耦合**

引擎层（src/）零 Web 依赖 → 可被 CLI 脚本、API 层、测试脚本独立调用。服务层不含业务逻辑，仅做请求校验和错误格式转换。

**优化 2：Prompt 外部化 + 8 条分层约束**

将硬编码在 Python 字符串中的 Prompt 迁移到 TOML 文件，8 条行为约束按优先级排序（零幻觉为最高优先级），修改即生效。

**优化 3：配置单例化 + 安全类型转换**

所有配置项收敛到 `Settings` 单例 dataclass。`_safe_int/_safe_float` 对环境变量做容错解析，解析失败有兜底值 + warning 而非崩溃。

**优化 4：MD5 哈希变更检测**

借鉴 Git object hash 机制，实现 --sync/--status/--dry-run 三种文档变更管理模式，解决"索引是否与源文档一致"的不确定性。

**优化 5：索引一致性自动校验**

启动时自动检测 embeddings/BM25/chunks_meta 跨文件记录数一致性，不一致自动裁剪防止越界崩溃。

### 4.4 核心优化思路总结

| 优化方向 | 核心思路 | 行业通用性 |
|---------|--------|----------|
| 检索召回 | 混合检索 + RRF 融合排序 + 增强分词 | RAG 系统标配，RRF 为 Elasticsearch 8.x+ 内置算法 |
| 语义完整性 | 五级切分（结构→段落→句子→合并→重叠）+ 章节类型标注 | 比固定 chunk_size 更通用，尤其适合中文结构化文档 |
| 检索评估 | 20题/6类意图 + Hit@K/MRR + LLM Judge + A/B对比 | 将 RAG 从"能用"推向"可量化可靠" |
| 资源控制 | 懒加载 + 轻量模型 + BLAS矩阵加速 | 从个人项目到生产环境均适用 |
| 可维护性 | Prompt 外部化 + 配置集中化 + 安全类型转换 | 降低 LLM 应用的迭代成本和配置风险 |
| 部署灵活性 | OpenAI 兼容协议统一接入 + MD5变更检测 | Provider 无关 + 索引一致性保障 |
| 数据质量 | 三级PDF级联 + 乱码率自动检测 + 后处理清洗 | 中文 PDF 生产环境的必需方案 |

---

## 模块五：系统化评估体系

### 5.1 评估指标与方法

| 维度 | 指标 | 方法 |
|------|------|------|
| 检索性能 | Hit Rate@1/3/5、MRR、Top1分数、双路命中 | 20 题测试集自动评测（`--extended`） |
| 生成质量 | 准确性/相关性/完整性（1-5分） | LLM-as-a-Judge 三维打分（`--judge`） |
| 鲁棒性 | Self-Reflection 重试前后对比 | `--judge --retry` 低分自动纠错 |
| 权重对比 | 5 种预设的 Hit Rate + MRR | `--compare` 一键 A/B 对比表 |
| 索引质量 | chunk 长度分布、文件覆盖、向量维度 | `evaluate_index()` 自动报告 |

### 5.2 评估命令

```bash
python -m src.evaluator                        # 5题基准 + 索引质量报告
python -m src.evaluator --extended             # 20题 + Hit@K + MRR + 分类统计
python -m src.evaluator --judge                # LLM三维打分
python -m src.evaluator --judge --retry        # + Self-Reflection 自动重试
python -m src.evaluator --compare              # A/B权重对比
python -m src.evaluator --question "自我介绍"   # 单条评估
```

---

## 模块六：项目核心亮点与求职竞争力总结

### 6.1 区别于普通学生项目的硬核亮点

1. **全手写 RAG 管道，零黑盒依赖**
   不依赖 LangChain/LlamaIndex 等封装框架，手写完整的检索-增强-生成管道。对 RAG 的每个环节都有代码级掌控。

2. **RRF 混合检索 + Min-Max 分数归一化**
   实现了 Dense（余弦相似度）+ Sparse（BM25增强分词）双路召回 + RRF 融合排序 + Min-Max 展示分数的完整检索链路。RRF 是 Elasticsearch 8.x+ 的内置工业标准融合算法。

3. **五级语义感知文本切分 + 章节类型标注**
   结构感知 → 段落优先 → 句子兜底 → 短块合并 → 块间 20% 重叠，配合 8 种章节类型自动标注，体现对文本处理底层逻辑的深刻理解。

4. **三级 PDF 级联解析引擎 + 乱码率自动检测**
   pdfplumber → pypdf → OCR，U+FFFD 乱码率 > 30% 自动降级，覆盖从电子版到扫描版的全质量 PDF。

5. **BM25 增强分词——73 自定义词 + 180 停用词**
   解决 jieba 默认分词对领域术语过度切分的痛点，构建和检索使用同一套 tokenize() 保证一致性。

6. **系统化评估体系——20 题/6 类意图 + LLM-as-a-Judge + A/B 对比**
   Hit Rate@K + MRR + LLM 三维打分 + Self-Reflection 重试 + 5 种权重预设对比，将 RAG 系统从"能用"推向"可量化可靠"。

7. **MD5 哈希变更检测与智能索引同步**
   借鉴 Git object hash 机制，实现 --sync/--status/--dry-run 三种模式，解决索引状态管理的不确定性。

8. **多 LLM Provider 热切换架构**
   基于 OpenAI 兼容协议实现了 Provider 无关的 LLM 接入层，支持 DeepSeek/OpenAI/私有化部署的无缝切换。

9. **纯 NumPy 实现向量检索 + Windows 兼容性攻克**
   放弃 ChromaDB（Windows 下 Rust FFI segfault），用 15 行 NumPy 代码实现完全可控的向量检索，体现环境兼容性优先的工程判断力。

10. **完整的工程化实践**
    三层架构分离、策略模式、单例模式、懒加载、防御性编程（5 个自动生成示例文件）、配置安全类型转换、索引一致性自动校验、分层错误处理——在企业级项目中被高频要求的工程实践均有落地。

### 6.2 对齐 Agent 开发岗位 JD 的核心能力映射

| 企业 JD 常见要求 | 本项目对应能力 |
|----------------|-------------|
| **RAG 系统设计与开发** | 全链路手写 RAG：三级PDF解析 → 五级切分+章节标注 → 双路索引 → RRF混合检索 → 上下文增强 → LLM生成 → 来源追溯 |
| **向量数据库/知识库** | NumPy 向量检索实现、BM25 增强关键词索引、索引持久化与 MD5 变更检测增量更新、一致性校验 |
| **Prompt Engineering** | TOML 外部化 Prompt 模板、角色/规则（8条）/模板三层架构、零幻觉安全护栏设计 |
| **LLM API 调用与多模型接入** | OpenAI 兼容协议统一接入、DeepSeek/OpenAI/Custom 三 Provider 热切换、懒加载客户端 |
| **智能 Agent 开发** | 检索增强 Agent、上下文记忆管理、基于事实的生成约束、LLM-as-a-Judge 自评估 |
| **Python 后端开发** | FastAPI 异步 API、Pydantic 类型校验、Streamlit 前端、三层架构分离 |
| **文本处理与 NLP** | jieba 增强分词（73自定义词+180停用词）、五级语义切分、多格式文档解析、章节类型标注 |
| **检索评估与调优** | 20题/6类意图测试集、Hit Rate@K+MRR、A/B权重对比、LLM Judge + Self-Reflection |
| **工程化能力** | 配置管理+安全类型转换、分层错误处理、防御性编程、MD5变更检测、索引一致性校验、策略/单例模式 |

### 6.3 技术能力与工程思维收获

**技术能力**：
- 掌握 RAG 系统的完整架构设计与工程实现，理解检索增强生成的技术原理与工业最佳实践（RRF 融合）
- 深入理解向量检索（余弦相似度、L2归一化、矩阵运算）与关键词检索（BM25、增强分词、停用词过滤）的融合策略
- 具备 Prompt Engineering 的体系化设计能力——从角色定义到 8 条分层行为约束到模板渲染
- 掌握中文 PDF 生产级解析方案（三级级联 + 乱码率检测 + OCR 降级）
- 掌握检索评估的系统化方法论（测试集设计、Hit@K/MRR、LLM Judge、A/B对比）
- 掌握 FastAPI 异步服务开发、Pydantic 类型安全校验、Streamlit 原型交付

**工程思维**：
- **技术方案匹配问题规模**：百级文档用 NumPy 不用 ChromaDB，千级文档用 MiniLM 不用 BGE-large，不过度工程化
- **环境兼容性优先于功能丰富度**：ChromaDB Windows 崩溃 → 自研方案，15行代码换 100% 可控
- **组件解耦优先**：引擎层与 Web 框架零依赖，任何前端均可接入，不绑定技术栈
- **防御性编程**：空目录自动生成示例、单文件失败不中断、配置错误有兜底、索引不一致自动裁剪
- **可维护性设计**：Prompt 外部化修改即生效、配置集中化安全转换、策略模式扩展解析器、清晰的分层架构
- **可量化评估**：20 题测试集 + 多维度指标 + A/B 对比，用数据驱动检索优化而非凭感觉

---

> 以上内容可直接按模块拆分用于：
> - **简历项目经历**：模块一（概述） + 模块三（核心功能摘要） + 模块六（亮点）
> - **项目自述/技术复盘**：完整六个模块
> - **面试技术问答**：模块二（技术选型理由） + 模块四（优化思路） + 模块五（评估体系）
