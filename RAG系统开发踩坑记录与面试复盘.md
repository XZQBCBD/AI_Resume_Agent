# RAG 系统开发踩坑记录与面试复盘

> 项目：个人 AI 数字分身（混合检索 RAG 系统）
> 目标岗位：AI Agent 应用开发工程师
> 每个问题按「背景 → 踩坑 → 根因 → 解决 → 面试话术」组织

---

## 一、向量数据库选型与兼容性

### 坑 1：ChromaDB 在 Windows 上 segfault 崩溃

**背景**：技术选型时选择了 ChromaDB（Python 生态最流行的轻量向量库），期望本地持久化零配置运行。

**踩坑**：在 Windows 10 + Python 3.11 环境下，`collection.add()` 在写入 SQLite 底层时稳定触发 segfault（exit code 139）。尝试了 ChromaDB 1.5.9 → 1.0.0 → 0.4.24 三个版本均失败。

**根因**：ChromaDB 1.x 使用 Rust 重写的 `chromadb_rust_bindings`，底层 SQLite 操作依赖 `pyo3` FFI。Windows 上缺少部分 Visual C++ 运行时组件时，Rust 编译的 `.pyd` 文件加载失败。0.4.x 使用 Python 原生 SQLite 但引入了 ONNX 运行时依赖链断裂问题。

**解决**：
1. 放弃 ChromaDB，自研 numpy + pickle 向量存储方案
2. `np.save("embeddings.npy")` 存向量，JSON 存元数据，pickle 存 BM25 索引
3. 余弦相似度用 numpy 手工实现：`np.dot(emb_norms, query_norm)`
4. 对小规模知识库（<1万条），numpy 方案比 ChromaDB 更快且零外部依赖

**面试话术**：
> "向量数据库选型时，我优先考虑了 ChromaDB 但发现它在 Windows 生产环境下有 Rust FFI 兼容性问题。我分析了根因——pyo3 绑定层与系统 C++ 运行时的不兼容。最终决定自研 numpy 方案，这个决策的 trade-off 是放弃了 HNSW 近似检索和分布式能力，但换来了零外部依赖、100% 可控的检索逻辑。实测小规模场景下 numpy 精确检索比 ChromaDB 更快。这个经历让我认识到选型时环境兼容性和可维护性比功能丰富度更重要。"

> **对应岗位要求**：向量数据库选型、系统设计 trade-off、工程落地能力

---

## 二、中文文档解析

### 坑 2：pypdf 中文 PDF 提取乱码

**背景**：用户上传的真实简历 PDF 包含中文文本层（非扫描版），期望 pypdf 直接提取文本。

**踩坑**：pypdf 提取的中文出现大量 U+FFFD 替换字符和乱码（如残缺的汉字和符号），但相同文件在 Mac 预览中可以正常复制文本。

**根因**：PDF 规范中 ToUnicode CMap 是可选的。当 PDF 生成工具（如某些国产 PDF 打印机）未嵌入 ToUnicode 映射表时，pypdf 只能回退到字体自身的编码表。中文字体的 CID 编码与 Unicode 之间没有标准映射，导致提取失败。pdfplumber 内部维护了更完整的 CMap 表，对中文支持更好。

**解决**：
1. 实现**三级级联解析引擎**：`pdfplumber → pypdf → OCR(pytesseract)`
2. pdfplumber 对中文 PDF 兼容性最好，作为首选引擎
3. 添加文本质量检测函数 `_calc_garbage_ratio()`：统计 U+FFFD 占比，>30% 自动触发 OCR 降级
4. 后处理清洗：正则移除页码残留（`^\d{1,3}$`），合并多余空行

**面试话术**：
> "处理中文 PDF 时，我发现 pypdf 对非标准 CMap 编码支持很差。我没有简单地换库，而是设计了一个三级级联解析策略：pdfplumber（中文最优）→ pypdf（回退）→ OCR（扫描版降级）。关键是加了文本质量自动检测——通过 U+FFFD 替换字符比例判断解析质量，超过 30% 自动触发 OCR。这套机制让系统对各种质量的 PDF 都有鲁棒性。"

> **对应岗位要求**：文档解析、RAG 数据预处理、多策略降级设计

---

## 三、网络与模型部署

### 坑 3：HuggingFace 被墙 + PyTorch DLL 兼容问题

**背景**：`sentence-transformers/all-MiniLM-L6-v2` 需从 huggingface.co 下载，国内网络直连超时。且 Windows 上 PyTorch 版本与 VC++ 运行时存在兼容矩阵。

**踩坑**：
1. huggingface.co 直连 WinError 10060 超时 → 配置 `HF_ENDPOINT=https://hf-mirror.com`
2. 镜像下载成功后，PyTorch 2.13.0 的 `c10.dll` 加载失败（WinError 1114）
3. 降级 PyTorch 2.5.1+cpu 解决 DLL 问题
4. `onnxruntime` 1.18.1 同样 DLL 失败，需精确锁定 `onnxruntime==1.17.1`

**根因**：Windows 上 PyTorch 的 C++ 扩展依赖特定版本的 Visual C++ Redistributable，不同版本对应不同 VC 运行时。镜像 `hf-mirror.com` 是 HuggingFace 官方推荐的国内镜像。

**解决**：
1. `.env` 中固化 `HF_ENDPOINT=https://hf-mirror.com`，`config.py` 通过 `load_dotenv()` 自动注入
2. 模型缓存路径通过劫持 `HF_HOME`/`TRANSFORMERS_CACHE`/`HUGGINGFACE_HUB_CACHE` 到非系统盘
3. `requirements.txt` 精确锁定 `torch==2.5.1+cpu`、`onnxruntime==1.17.1`
4. 模型一次下载后本地缓存复用

**面试话术**：
> "在国内部署 HuggingFace 模型遇到两个典型问题：网络访问和 DLL 兼容。网络方面我通过 HF_ENDPOINT 配置国内镜像并写入 .env 实现自动注入。PyTorch DLL 方面，我理解了不同版本依赖不同 VC++ 运行时，通过精确版本锁定解决。此外我把所有模型缓存路径劫持到非系统盘——这一点在面试演示时特别重要，面试官可能用 U 盘携带你的项目。"

> **对应岗位要求**：大模型部署、环境管理、依赖锁定、国内部署最佳实践

---

## 四、RAG 检索质量优化

### 坑 4：BM25 召回不稳定——0 条到 10 条的极端波动

**背景**：混合检索中 BM25 通路召回量剧烈波动。"自我介绍"命中 0 条，"技术栈"命中 10 条。0 条时 BM25 完全失效，融合退化为纯向量检索。

**踩坑**：
1. jieba 默认分词把"自我介绍"切成"自我"+"介绍"，两个独立 token 在文档中匹配不到
2. 无停用词过滤，"的""了""是"等高频词产生噪声分数
3. BM25 分数无阈值过滤，低分噪声也被计入 Top-10

**根因**：jieba 默认词典是通用场景训练的，对简历/技术领域复合词过度切分。BM25 的 TF-IDF 本质决定了高频无意义词会稀释有效词权重。

**解决**：
1. 建立 **73 个自定义词表**（技术术语 + 简历场景词），用 `jieba.add_word()` 注册
2. 建立 **180 个中英文停用词表**，`tokenize()` 时过滤
3. 添加 `BM25_SCORE_THRESHOLD=0.05` 分数阈值
4. 关键：构建索引和检索时使用**同一套 `tokenize()` 函数**（抽取到 `bm25_utils.py`），保证分词一致性

**面试话术**：
> "BM25 召回不稳定是 RAG 优化的一个典型问题。我分析发现根因有两个：jieba 默认分词对领域术语切分过细，以及缺少停用词过滤导致噪声稀释有效信号。我的方案是建立领域自定义词典和停用词表，同时加入分数阈值。这里有一个容易被忽略的关键点——构建索引和查询时必须使用同一套分词器，否则会出现词表不一致导致召回失败。我专门抽了一个 bm25_utils 模块来保证一致性。"

> **对应岗位要求**：搜索相关性优化、NLP 分词、BM25 调优

---

### 坑 5：文档切分导致信息碎片化——块长度两极分化

**背景**：首次切分后 <200 字符短块和 >500 字符长块各占 1/3。短块信息不足，长块上下文冗余。

**踩坑**：
1. 简历 PDF 的列表项（如"· 主导公司智能客服..."）每行被单独切为一个短块
2. 超长段落中的单句 >500 字符时无法切分，保留为超长块
3. 切分未考虑文档视觉结构（列表、标题），破坏了语义完整性

**根因**：简单的"按段落切分 + 按句子边界二次切分"没有考虑文档结构信息和块长度平衡。

**解决**：
1. **结构感知切分**：正则识别列表项（`1.`、`-`、`·`、`第X章`）和标题（`##`）作为硬边界
2. **短块合并**：`_merge_short_chunks()` 将 <200 字符的块与相邻块合并
3. **块间重叠 20%**：`chunk_overlap=100`，相邻块保留重叠区域避免边界信息丢失
4. 合并后长度分布从两极分化改善为集中在 200-500 区间

**面试话术**：
> "文本切分策略对 RAG 检索质量影响巨大。我最初用简单的段落切分，结果块长度两极分化。我做了三个改进：第一，结构感知——用正则识别列表项和标题作为硬边界；第二，短块合并——把 <200 字符的碎片与相邻块合并；第三，块间重叠 20%——确保跨块边界的信息不丢失。这个优化让 Top1 分数从固定值变得有区分度，检索结果更有层次。"

> **对应岗位要求**：RAG chunking 策略、文档预处理、信息检索

---

### 坑 6：Dense/Sparse 权重调优缺乏系统方法

**背景**：初始权重 Dense=0.6 / Sparse=0.4，Top1 分数固定 0.60，双路命中率仅 12%，检索结果缺乏区分度。

**踩坑**：
1. 权重调优全靠"感觉"，没有 A/B 测试框架
2. Dense 权重过高时融合分数被向量相似度主导，Top1 几乎不变
3. 双路命中率 12% 意味着两条通路检索到的是几乎不重叠的文档集合，融合策略形同虚设

**根因**：缺少科学的评估体系和参数对比机制，调参过程完全盲目。

**解决**：
1. 设计 **5 种权重预设**（balanced/semantic_first/keyword_first/dense_only/sparse_only）
2. 实现 `python -m src.evaluator --compare` A/B 对比命令
3. 增加 **Hit Rate@K + MRR** 指标替代单纯的平均分数
4. 设计 **20 题/6 类意图**测试集，每类意图独立统计 Top1分数、Sparse召回、双路命中

**面试话术**：
> "混合检索的权重调优不能靠感觉。我设计了一套 A/B 测试框架：5 种权重预设、20 题测试集覆盖 6 类意图、Hit Rate@K 和 MRR 多维度指标。通过对比发现 balanced(0.5/0.5) 在大多数场景下表现最好。这个框架的复用价值在于——换了知识库或检索场景后，只需跑一次对比就能确定最优权重。"

> **对应岗位要求**：超参数调优、评估方法设计、检索质量度量

---

## 五、工程化与运维

### 坑 7：Windows 路径转义导致神秘崩溃

**背景**：`config.py` 中使用 Windows 风格路径，运行时抛出 `OSError: [WinError 123]`。

**踩坑**：Python 字符串中 `\a` 是 BEL 转义字符（ASCII 7 = `\x07`），`\v` 是垂直制表符。路径中的 `\a` 和 `\v` 被 Python 解释为不可打印控制字符，导致路径无效。

**根因**：Python 字符串字面量中反斜杠是转义前缀。Windows 路径使用反斜杠作为分隔符，与 Python 转义机制冲突。即使在 dataclass 的 field default 中使用，Python 解释器在解析字符串字面量时就已经处理了转义。

**解决**：
1. **全局规则：所有路径字符串使用正斜杠**：`"E:/AI_Cache/huggingface"`
2. Path 对象的 `/` 运算符拼接：`root / "data" / "raw"`
3. Path 对象在 Windows 上自动转换为反斜杠，但源码中始终用正斜杠
4. 这同时解决了 Linux/Mac 跨平台兼容性

**面试话术**：
> "Python 在 Windows 上处理路径时，最隐蔽的坑是反斜杠转义。`\a`、`\v`、`\n` 在 Windows 路径中看起来无害，但在 Python 字符串中会被解释为转义序列。我定的规则是代码中全部使用正斜杠，让 pathlib 在运行时自动转换。这个习惯对跨平台兼容也很重要。"

> **对应岗位要求**：Python 工程能力、跨平台开发、防御性编程

---

### 坑 8：.pyc 缓存导致代码修改"不生效"

**背景**：修改 `config.py` 后重新运行，错误仍然指向旧路径。调试很久才发现是 `__pycache__` 的旧字节码没有更新。

**踩坑**：Python 的 `__pycache__` 目录缓存了修改前的 `.pyc` 字节码。当源文件修改后，如果 `.pyc` 时间戳没有正确更新（跨文件系统、虚拟机、或快速修改场景），Python 解释器可能继续使用旧字节码。

**根因**：`.pyc` 文件时间戳粒度是秒级，快速修改文件再运行时可能命中同一秒的旧缓存。在某些文件系统（如 WSL 和 Windows 之间的跨文件系统访问）中时间戳可能不准确。

**解决**：
1. 每次关键修改后执行 `find . -type d -name "__pycache__" -exec rm -rf {} +`
2. 启动脚本可使用 `python -B` 禁用 `.pyc` 写入

**面试话术**：
> "Python 的 .pyc 缓存机制在开发阶段可能造成代码修改不生效的假象。我排查了很久才发现是 __pycache__ 的旧字节码。现在我的习惯是配置变更后一定先清缓存，避免被缓存误导排查方向。"

> **对应岗位要求**：Python 运行时机制理解、问题排查能力

---

### 坑 9：索引更新缺少变更检测——不知道索引是否最新

**背景**：用户增/删/改 `data/raw/` 下的文档后，需手动判断应该增量还是全量重建索引。没有机制回答"索引是否与文档一致"。

**踩坑**：
1. 增量模式只追加新 chunk，不处理删除和修改
2. 全量模式每次都重新嵌入，对未修改的文档浪费算力
3. 无变更追踪，用户无法确认索引状态

**根因**：索引构建和文档管理之间缺少状态同步机制。需要一种方式来判断"索引是否与源文档一致"。

**解决**：
1. 引入 **MD5 文件哈希追踪**：每次成功构建后保存 `file_hashes.json` 快照
2. 实现 `--sync` 模式：对比哈希 → 检测增/删/改 → 有变更则全量重建
3. 实现 `--status` 模式：仅检测变更不操作，零开销查看状态
4. 实现 `--sync --dry-run`：预览将要执行的操作
5. 设计思路借鉴了 Git 的 object hash 机制

**面试话术**：
> "RAG 系统的索引更新策略容易被忽视。我设计了一套基于 MD5 哈希的变更检测机制：每次构建后保存文件哈希快照，下次构建时对比差异，只有检测到变更时才触发重建。这保证了索引与文档的一致性又避免了不必要的重复计算。这个思路借鉴了 Git 的 object hash 机制。"

> **对应岗位要求**：数据管道设计、增量更新、状态管理

---

### 坑 10：OpenAI SDK 隐式依赖 jiter 缺失

**背景**：LLM 调用时报 `ModuleNotFoundError: No module named 'jiter'`，但 `requirements.txt` 中未声明此依赖。

**踩坑**：`jiter` 是 OpenAI Python SDK 的传递依赖（Rust 编译的 JSON 流式解析器），在某些 Windows 环境下 `pip install` 时编译失败，pip 静默跳过，运行时才发现缺失。

**根因**：pip 的依赖解析在某些场景下不严格。传递依赖的安装失败可能被静默忽略。`jiter` 是 Rust + PyO3 编译的扩展，在缺少 Rust 编译器的 Windows 环境可能失败。

**解决**：
1. `requirements.txt` 中显式声明 `jiter>=0.5.0`
2. 使用 `pip check` 验证依赖完整性
3. 传递依赖也应显式声明，尤其是涉及原生编译的包

**面试话术**：
> "OpenAI SDK 有一个隐式依赖 jiter 在特定环境下安装失败但 pip 不报错。教训是：关键传递依赖要显式声明在 requirements.txt 中，尤其是涉及原生编译的包。pip check 是检查依赖完整性的好工具。"

> **对应岗位要求**：Python 依赖管理、生产环境调试

---

## 六、面试技能映射速查表

| 踩坑 | 对应岗位高频要求 | 关键词 |
|------|-----------------|--------|
| 坑 1 | 向量数据库选型与 trade-off | ChromaDB, numpy, 兼容性 |
| 坑 2 | RAG 文档预处理 | PDF 解析, OCR, 中文 NLP |
| 坑 3 | 大模型部署与工程化 | HuggingFace, PyTorch, 环境管理 |
| 坑 4 | 搜索相关性优化 | BM25, jieba, 停用词, 分词 |
| 坑 5 | 文档切分/chunking 策略 | 语义切分, 重叠, 块平衡 |
| 坑 6 | 检索评估与 A/B 测试 | Hit@K, MRR, 权重调优 |
| 坑 7 | Python 跨平台工程能力 | 路径处理, 转义字符 |
| 坑 8 | Python 运行时机制 | .pyc 缓存, 字节码 |
| 坑 9 | 数据管道与增量更新 | 哈希追踪, 变更检测 |
| 坑 10 | 依赖管理与生产部署 | pip, 传递依赖 |

---

## 七、STAR 法则面试回答模板

**Q: 你在 RAG 系统开发中遇到过最大的技术挑战是什么？**

> **S (情境)**: 我独立开发了一个个人 AI 数字分身系统，用于求职面试场景。需要支持中文简历 PDF 的混合检索 RAG 管道，技术栈要求纯 Python、本地运行、跨平台兼容。
>
> **T (任务)**: 核心挑战有三个层面——数据层（中文 PDF 解析质量）、检索层（BM25 召回不稳定 + 权重调优缺乏方法）、工程层（向量数据库 Windows 兼容性）。
>
> **A (行动)**:
> - **PDF 解析**：设计了 pdfplumber → pypdf → OCR 三级级联引擎，加入 U+FFFD 乱码率自动检测（阈值 30%），乱码率从可见乱码优化到 0%
> - **BM25 优化**：建立 73 个技术领域自定义词 + 180 个停用词，添加分数阈值过滤，设计 5 种权重预设和 A/B 对比框架
> - **向量存储**：分析 ChromaDB Rust FFI 根因后决定自研 numpy 方案，换取零依赖和 100% 可控性
> - **评估体系**：设计 20 题/6 类意图测试集，引入 Hit Rate@K 和 MRR 指标，支持一键 A/B 对比
>
> **R (结果)**: Top1 分数从固定 0.60 优化到有区分度的 0.50-1.00 范围，Hit Rate@5 达 90%，MRR 0.54。系统支持一键启动，多 LLM Provider 切换，MD5 哈希智能增量同步。

---

> 附录：[2026年AI Agent应用开发工程师岗位要求与招聘趋势](https://blog.csdn.net/2301_80630163/article/details/161491117)
